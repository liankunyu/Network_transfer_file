read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi

read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi

read -p "please input you want find nameid:" A
if grep ^$A /etc/passwd;then
	echo "The account already exists"
	exit 0
else
	echo "The account does not exist"
	exit 4
fi
